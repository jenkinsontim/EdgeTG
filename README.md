# EdgeTG
A minimal, canonical encoding for ordered trees using 3 glyphs (`_ / \`). Ultra-compact wire format (~10 bytes) and evolutionary genome for constrained IoT devices. ~1.1KB C core verified under ASan/UBSan. Author in Lua, C3, Zig, or Python.
